# Image Search Engine

## Overview
The **Image Search Engine** is a web application that allows users to search for images using keywords. It fetches images from an external API based on the user's search query and displays the results in a grid format. This project is built using HTML, CSS, and JavaScript.

## Features
- **Search Images**: Users can search for images by entering keywords.
- **Display Results**: Images are displayed in a responsive grid layout.
- **Dynamic Loading**: Images are loaded dynamically based on user input.
- **Responsive Design**: The webpage is fully responsive and works well on both desktop and mobile devices.

## Technologies Used
- **HTML5**: Structure of the webpage.
- **CSS3**: Styling and layout.
- **JavaScript**: Functionality for handling user input and fetching images from an external API.
- **API Integration**: Fetch images from an external image search API (e.g., Unsplash API, Pixabay API).

## Installation
To run this project locally:

1. **Clone the repository:**
   ```bash
   git clone https://github.com/Ramya200312/Image-Search-Engine.git
